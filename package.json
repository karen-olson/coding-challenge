{
  "name": "shure-coding-challenge",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:watch": "vitest watch",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "prettier": "^3.3.1",
    "typescript": "^5.2.2",
    "vite": "^5.2.0",
    "vitest": "^3.1.3"
  }
}
